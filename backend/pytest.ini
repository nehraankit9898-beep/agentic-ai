[pytest]
testpaths = tests
asyncio_mode = auto
# -p no:libtmux: the libtmux pytest plugin bundled in some environments is
# incompatible with pytest>=8 (applies marks to fixtures) and crashes
# collection. Disable it so our own suite can run.
addopts = -q -p no:libtmux
